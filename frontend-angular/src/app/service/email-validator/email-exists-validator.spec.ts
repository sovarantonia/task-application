import { EmailExistsValidator } from './email-exists-validator';

describe('EmailExistsValidator', () => {
  it('should create an instance', () => {
    expect(new EmailExistsValidator()).toBeTruthy();
  });
});
